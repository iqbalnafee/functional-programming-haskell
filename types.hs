fType:: Float -> Float -> Float
fType x y = x*x + y*y
--main = print(fType 2 4)
main =  do 
    if 8 /= 8 -- /= equivalen to not equal to 
        then putStrLn "this values are equivalent"
    else 
        putStrLn "they are not equivalent"